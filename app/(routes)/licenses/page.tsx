import Container from "@/components/Container";
import React from "react";

type Props = {};

const LicensesPage = (props: Props) => {
  return (
    <Container title="Licenses" description={""}>
      Licenses
    </Container>
  );
};

export default LicensesPage;
